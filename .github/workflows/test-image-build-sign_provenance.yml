name: Test Image Building With Signature and Provenance

"on":
  push:
  workflow_dispatch: {}

permissions:
  contents: read
  packages: write
  id-token: write
  actions: read

env:
  IMAGE_REGISTRY: ghcr.io
  IMAGE_REPOSITORY: aicage/test-image-sign_provenance
  VERSION_TAG: version-${{ github.sha }}
  PURPOSE_TAG: latest
  TEST_TAG_PREFIX: ""
  TEST_TAG_POSTFIX: -test
  DOCKERFILE: test-image-sign_provenance/Dockerfile
  BUILD_CONTEXT: test-image-sign_provenance/
  GH_TOKEN: ${{ github.token }}

jobs:

  set-globals:
    name: Set global vars
    runs-on: ubuntu-latest
    outputs:
      IMAGE_REF: ${{ steps.output-vars.outputs.IMAGE_REF }}
      UNIQUE: ${{ steps.output-vars.outputs.UNIQUE }}
    steps:
      - name: Output global vars
        id: output-vars
        run: |
          set -euo pipefail
          {
            echo "IMAGE_REF=${IMAGE_REGISTRY}/${IMAGE_REPOSITORY}"
            echo "UNIQUE=${{ github.sha }}-$(uuidgen)"
          } | tee /dev/stderr >> "$GITHUB_OUTPUT"

  build:
    name: Build, sign, and publish image (${{ matrix.arch }})
    runs-on: ${{ matrix.runner }}
    needs: set-globals
    strategy:
      matrix:
        include:
          - arch: amd64
            platform: linux/amd64
            runner: ubuntu-latest
          - arch: arm64
            platform: linux/arm64
            runner: ubuntu-24.04-arm
    env:
      ARCH_SUFFIX: ${{ matrix.arch }}
      PLATFORM: ${{ matrix.platform }}
      IMAGE_REF: ${{ needs.set-globals.outputs.IMAGE_REF }}
      UNIQUE: ${{ needs.set-globals.outputs.UNIQUE }}
    outputs:
      INDEX_DIGEST_amd64: ${{ steps.out-amd64.outputs.INDEX_DIGEST }}
      INDEX_DIGEST_arm64: ${{ steps.out-arm64.outputs.INDEX_DIGEST }}

    steps:

      - name: Write image tags to env
        run: |
          set -euo pipefail
          {
            echo "IMAGE_TEST_TAG=${IMAGE_REF}:${TEST_TAG_PREFIX}${UNIQUE}-${{ matrix.arch }}${TEST_TAG_POSTFIX}"
          } | tee /dev/stderr >> "$GITHUB_ENV"

      - name: Checkout
        uses: actions/checkout@v6.0.1

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3.11.1
        with:
          driver: docker-container

      - name: Log in to Docker Registry
        uses: docker/login-action@v3.6.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build test image
        id: build_image
        uses: docker/build-push-action@v6
        with:
          context: ${{ env.BUILD_CONTEXT }}
          file: ${{ env.DOCKERFILE }}
          push: true
          provenance: true
          platforms: ${{ env.PLATFORM }}
          tags: |
            ${{ env.IMAGE_TEST_TAG }}

      - name: Test image
        uses: addnab/docker-run-action@v3
        with:
          registry: ${{ env.IMAGE_REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
          image: ${{ env.IMAGE_REF }}@${{ steps.build_image.outputs.digest }}
          run: |
            set -euo pipefail
            echo "Debug: cat /test.txt"
            cat /test.txt
            grep -q 'test-date:' /test.txt

      - name: Export digest (amd64)
        id: out-amd64
        if: matrix.arch == 'amd64'
        run: echo "INDEX_DIGEST=${{ steps.build_image.outputs.digest }}" >> "$GITHUB_OUTPUT"

      - name: Export digest (arm64)
        id: out-arm64
        if: matrix.arch == 'arm64'
        run: echo "INDEX_DIGEST=${{ steps.build_image.outputs.digest }}" >> "$GITHUB_OUTPUT"

  cosign-signature:
    name: Add cosign signature (${{ matrix.arch }})
    needs: [set-globals, build]
    runs-on: ubuntu-latest
    env:
      IMAGE_REF: ${{ needs.set-globals.outputs.IMAGE_REF }}
    strategy:
      matrix:
        include:
          - arch: amd64
            INDEX_DIGEST: ${{ needs.build.outputs.INDEX_DIGEST_amd64 }}
          - arch: arm64
            INDEX_DIGEST: ${{ needs.build.outputs.INDEX_DIGEST_arm64 }}

    steps:

      - name: Write image info to env
        run: |
          set -euo pipefail
          INDEX_DIGEST=${{ matrix.INDEX_DIGEST }}
          PLATFORM_DIGEST="$(docker buildx imagetools inspect --raw "${IMAGE_REF}@${INDEX_DIGEST}" \
            | jq -r '.manifests[]
              | select(.platform.os=="linux" and .platform.architecture=="${{ matrix.arch }}")
              | .digest' | head -n1)"
          {
            echo "INDEX_DIGEST=${INDEX_DIGEST}"
            echo "PLATFORM_DIGEST=${PLATFORM_DIGEST}"
          } | tee /dev/stderr >> "$GITHUB_ENV"

      - name: Log in to Docker Registry
        uses: docker/login-action@v3.6.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Install Cosign
        uses: sigstore/cosign-installer@v4.0.0

      - name: Sign image with cosign
        run: |
          set -euo pipefail
          # buildx often returns an OCI *index* digest even for single-arch builds.
          # Sign BOTH:
          #   1) the index digest (what the tag resolves to)
          #   2) the platform manifest digest (so `cosign verify IMAGE@PDIGEST` works)
          test -n "${INDEX_DIGEST}"
          cosign sign --yes "${IMAGE_REF}@${INDEX_DIGEST}"
          test -n "${PLATFORM_DIGEST}"
          cosign sign --yes "${IMAGE_REF}@${PLATFORM_DIGEST}"

      - name: Verify image signature
        run: |
          set -euo pipefail
          cosign verify \
            --certificate-oidc-issuer "https://token.actions.githubusercontent.com" \
            --certificate-identity-regexp \
              "^https://github.com/${GITHUB_REPOSITORY}/.github/workflows/.*@refs/.*/.*$" \
            "${IMAGE_REF}@${INDEX_DIGEST}"

          # Also verify the platform manifest digest is signed.
          test -n "${PLATFORM_DIGEST}"
          cosign verify \
            --certificate-oidc-issuer "https://token.actions.githubusercontent.com" \
            --certificate-identity-regexp \
              "^https://github.com/${GITHUB_REPOSITORY}/.github/workflows/.*@refs/.*/.*$" \
            "${IMAGE_REF}@${PLATFORM_DIGEST}"

  slsa-provenance:
    name: SLSA provenance (${{ matrix.arch }})
    needs: [set-globals, build]
    uses: slsa-framework/slsa-github-generator/.github/workflows/generator_container_slsa3.yml@v2.1.0
    strategy:
      matrix:
        include:
          - arch: amd64
            INDEX_DIGEST: ${{ needs.build.outputs.INDEX_DIGEST_amd64 }}
          - arch: arm64
            INDEX_DIGEST: ${{ needs.build.outputs.INDEX_DIGEST_arm64 }}
    with:
      image: ${{ needs.set-globals.outputs.IMAGE_REF }}
      digest: ${{ matrix.INDEX_DIGEST }}
      registry-username: ${{ github.actor }}
    secrets:
      registry-password: ${{ secrets.GITHUB_TOKEN }}

  verify-provenance:
    name: Verify provenance (${{ matrix.arch }})
    needs: [set-globals, build]
    runs-on: ubuntu-latest
    env:
      IMAGE_REF: ${{ needs.set-globals.outputs.IMAGE_REF }}
    strategy:
      matrix:
        include:
          - arch: amd64
            INDEX_DIGEST: ${{ needs.build.outputs.INDEX_DIGEST_amd64 }}
          - arch: arm64
            INDEX_DIGEST: ${{ needs.build.outputs.INDEX_DIGEST_arm64 }}

    steps:

      - name: Write image info to env
        run: |
          set -euo pipefail
          ATTESTATION_DIGEST="$(docker buildx imagetools inspect --raw "${IMAGE_REF}@${INDEX_DIGEST}" \
            | jq -r '.manifests[]
              | select(.annotations."vnd.docker.reference.type"=="attestation-manifest")
              | .digest' | head -n1)"
          {
            echo "ATTESTATION_DIGEST=${ATTESTATION_DIGEST}"
          } | tee /dev/stderr >> "$GITHUB_ENV"

      - name: Verify provenance is present (and pinned by the signed index)
        run: |
          set -euo pipefail
          test -n "${ATTESTATION_DIGEST}"
          # Must contain an in-toto layer declaring a SLSA provenance predicate-type.
          docker buildx imagetools inspect --raw "${IMAGE_REF}@${ATTESTATION_DIGEST}" \
            | jq -e '.layers[]?
              | .annotations."in-toto.io/predicate-type"
              | select(startswith("https://slsa.dev/provenance/"))'

  publish-image:
    name: Publish image (${{ matrix.arch }})
    needs: [set-globals, build, cosign-signature, slsa-provenance, verify-provenance]
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - arch: amd64
            INDEX_DIGEST: ${{ needs.build.outputs.INDEX_DIGEST_amd64 }}
          - arch: arm64
            INDEX_DIGEST: ${{ needs.build.outputs.INDEX_DIGEST_arm64 }}
    steps:
      - name: Write image tags to env
        run: |
          set -euo pipefail
          {
            echo "IMAGE_VERSION_TAG=${IMAGE_REF}:${VERSION_TAG}-${{ matrix.arch }}"
            echo "IMAGE_PURPOSE_TAG=${IMAGE_REF}:${PURPOSE_TAG}-${{ matrix.arch }}"
          } | tee /dev/stderr >> "$GITHUB_ENV"
      - name: Publish architecture-specific tags
        run: |
          set -euo pipefail
          docker buildx imagetools create \
            --tag "${IMAGE_VERSION_TAG}" \
            --tag "${IMAGE_PURPOSE_TAG}" \
            "${IMAGE_REF}@${INDEX_DIGEST}"

  manifest:
    name: Publish multi-arch manifest
    runs-on: ubuntu-latest
    needs: [set-globals, build, publish-image]
    env:
      IMAGE_REF: ${{ needs.set-globals.outputs.IMAGE_REF }}
      UNIQUE: ${{ needs.set-globals.outputs.UNIQUE }}
      INDEX_DIGEST_amd64: ${{ needs.build.outputs.INDEX_DIGEST_amd64 }}
      INDEX_DIGEST_arm64: ${{ needs.build.outputs.INDEX_DIGEST_arm64 }}

    steps:

      - name: Write manifest tags to env
        run: |
          set -euo pipefail
          {
            echo "MANIFEST_VERSION_TAG=${IMAGE_REF}:${VERSION_TAG}"
            echo "MANIFEST_PURPOSE_TAG=${IMAGE_REF}:${PURPOSE_TAG}"
            echo "MANIFEST_TEST_TAG=${IMAGE_REF}:${UNIQUE}-test"
          } | tee /dev/stderr >> "$GITHUB_ENV"

      - name: Checkout
        uses: actions/checkout@v6.0.1

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3.11.1
        with:
          driver: docker-container

      - name: Log in to Docker Hub
        uses: docker/login-action@v3.6.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish test multi-arch manifest
        run: |
          set -euo pipefail
          docker buildx imagetools create \
            --tag "${MANIFEST_TEST_TAG}" \
            "${INDEX_DIGEST_amd64}" \
            "${INDEX_DIGEST_arm64}"

      - name: Install Cosign
        uses: sigstore/cosign-installer@v4.0.0

      - name: Sign multi-arch manifest
        run: |
          set -euo pipefail
          cosign sign --yes \
            "${MANIFEST_TEST_TAG}"

      - name: Verify multi-arch manifest signature
        run: |
          set -euo pipefail
          cosign verify \
            --certificate-oidc-issuer "https://token.actions.githubusercontent.com" \
            --certificate-identity-regexp \
              "^https://github.com/${GITHUB_REPOSITORY}/.github/workflows/.*@refs/.*/.*$" \
            "${MANIFEST_TEST_TAG}"

      - name: Publish final multi-arch manifest tags
        run: |
          set -euo pipefail
          docker buildx imagetools create \
            --tag "${MANIFEST_VERSION_TAG}" \
            --tag "${MANIFEST_PURPOSE_TAG}" \
            "${MANIFEST_TEST_TAG}"
